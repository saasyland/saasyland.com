---
title: "Paramètres"
description: "L’écran /admin/settings : un onglet sécurité réel et un onglet général statique."
---

`src/routes/admin.settings.tsx` affiche deux onglets composés de widgets de `src/presentation/components/custom/admin/settings`. Le loader précharge `getActiveSessionsQuery`. L’espace client `/app` n’a pas d’écran de paramètres.

## Sécurité (réel)

- `SettingsPasswordForm` demande le mot de passe actuel, le nouveau et sa confirmation, et appelle `changePassword` (`src/modules/account/use-cases/change-password.ts`, connecté et limité). En cas de succès, la liste des sessions est actualisée.
- `SettingsTwoFactor` active ou désactive TOTP ([Authentification à deux facteurs](/docs/features/two-factor)).
- `SettingsSessions` liste et révoque les sessions ([Sessions](/docs/features/sessions)).

## Général (démo)

- `ProfileCard` : nom de l’espace de travail, e-mail d’assistance et URL, préremplis avec `APP_NAME` et des constantes du fichier.
- `PreferencesCard` : langue, fuseau horaire et un interrupteur pour les annonces.
- `DangerZoneCard` : un bouton de suppression.

Ces cartes affichent uniquement des valeurs par défaut ; rien n’est enregistré. `updateUser` et `changeEmail` dans `src/modules/account/use-cases` sont les fonctions serveur à brancher lorsque vous créez un formulaire de profil.
