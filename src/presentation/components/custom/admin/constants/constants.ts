export const AVATAR_INITIALS_LENGTH = 2

export const PAGINATION_FIRST_PAGE = 1
const PAGINATION_PAGE_COUNT = 3

export const PAGINATION_PAGES = Array.from({ length: PAGINATION_PAGE_COUNT }, (_, index) => PAGINATION_FIRST_PAGE + index)
