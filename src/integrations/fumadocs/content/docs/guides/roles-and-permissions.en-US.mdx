---
title: "Roles & permissions"
description: "RBAC statements, ROLE_CODES, and permission checks."
---

## Source of truth

`integrations/better-auth/auth.access.ts`

- `ROLE_CODES` / `ROLE_VALUES` / `DEFAULT_ROLE_CODE`
- `APP_STATEMENTS` resource actions, composed with the Better Auth admin defaults into `ROLES`
- `Permission` type + `hasPermission(role, permission)` — pure and fail-closed
- The `admin: ["access"]` statement gates the admin console (checked in `proxy.ts`)

Mutations attach checks at the definition site via `actionClient.use(withAuth({ user: ["ban"] }))`; RSC reads call `hasPermission` after `getCurrentSession()`.
