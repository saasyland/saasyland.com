---
title: "Uwierzytelnianie"
description: "Better Auth, hasło i OAuth."
---

> Pełna treść jest utrzymywana po angielsku i może być uzupełniana sukcesywnie. Poniżej znajduje się lokalna wersja / mapa tej strony.

## Ownership

`integrations/better-auth` — `auth.server.ts`, `auth.client.ts`, validations, errors, guards.

## Capabilities

- E-mail i hasło
- OAuth GitHub + Google
- Ciasteczka sesji przez Better Auth
- Ochrona `/admin` i `/app` wymuszana przez proxy

## Extend

Add social providers in auth server config; keep secrets in env; update docs + `.env.example`.
