---
title: "Changelog"
description: "Mudanças relevantes ao longo do tempo."
---

## 2026-09-26

- Internacionalização reconstruída em torno dos namespaces do use-intl: um arquivo JSON por namespace em `messages/<locale>/`, um `NAMESPACE` por rota de página e `NAMESPACES` por rota de layout, namespaces raiz para todas as telas e uma etapa `check:i18n` que resolve cada chave estática no escopo do seu `useTranslations`.
- E-mails divididos em templates do React Email (apenas markup e namespace) e casos de uso que carregam as mensagens e fazem o envio por meio de `sendEmail`.
- Componentes reorganizados: os arquivos de rota compõem as páginas, as pastas de componentes são planas e cada widget, diálogo, formulário e conjunto de colunas de tabela tem o próprio arquivo.
- Todos os formulários seguem o padrão shadcn TanStack Form com `Field`, `FieldLabel`, `Input` e os schemas Zod dos módulos.
- Um único `DataTable` genérico sobre o TanStack Table v9 substitui as implementações de tabela individuais.
- Formulário de newsletter mais simples.
- Corrigido: os links de navegação e de seção mantêm o locale em páginas diferentes da página inicial.
- Corrigido: os rótulos dos gráficos do painel de administração são localizados.
- Verificação de cobertura do Vitest em 100% para linhas, instruções, funções e branches.

## Anteriormente

- Camada de casos de uso modular em `src/modules`
- DataTable de usuários do admin com dados reais + abas com nuqs
- Práticas de Suspense compatíveis com Cache Components
- Mapa abrangente da documentação (este site)
- Toolchain Vite+ e verificações rigorosas de cobertura

Adicione seções datadas à medida que publicar novas versões.
