---
title: "Composition patterns"
description: "Prefer composition over boolean props; lift state into providers."
---

Use compound components and focused providers to share state. Keep reusable UI in `src/presentation/components`, place feature-specific parts beside their parent component, and pass refs as props with React 19. Keep view logic separate from feature use cases in `src/modules`.
