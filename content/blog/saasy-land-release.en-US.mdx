---
title: "SaaSy Land 2.0: The TanStack Start SaaS Starter With a 100% Coverage Gate"
description: "A production React SaaS starter with TanStack Start and Cloudflare. Auth on your own database, billing, 9 locales, a page designer, and a coverage gate that fails the build. $249 once."
excerpt: "One command scaffolds auth, billing, email, an admin and a marketing site, with 176 test files and a coverage gate already enforcing them. Here is exactly what ships, and how to check every number before you pay."
authorName: Piotr J. Borowiecki
authorImage: https://avatars.githubusercontent.com/u/27385980?s=96&v=4
date: 2026-08-15
updated: 2026-09-06
image: /images/blog/saasy-land-release.webp
tags:
  - saas
  - boilerplate
  - tanstack-start
  - release
faq:
  - question: What is SaaSy Land?
    answer: >-
      SaaSy Land is a production React SaaS starter kit. A CLI asks configuration questions and scaffolds
      only what you answered: authentication on your own database, billing, transactional email, an
      admin console, 9-locale internationalization and a visual page designer, with 176 test files
      and a 100% coverage gate already enforcing them in CI. One payment of $249, no per-user fees.
  - question: How much does SaaSy Land cost?
    answer: >-
      Three one-time tiers. Core is $249 and includes editable generated app source, a CLI licence and lifetime core
      updates. Complete is $399 and adds the full video course. Agency is $899 and adds an
      unlimited client licence, priority Discord support and Figma files. There is no subscription or monthly active user meter. Activation and any seat limits are disclosed before purchase.
  - question: Is SaaSy Land really 100% test coverage?
    answer: >-
      The public website has 176 test files and a 100% threshold for statements, branches, functions and lines in the code included by the coverage configuration. Route glue, generated files and selected presentation code are excluded explicitly in vite.config.ts. Eight Playwright suites exercise real Chromium and WebKit separately. Run bun run test:coverage to regenerate the report.
  - question: What tech stack does SaaSy Land 2.0 use?
    answer: >-
      The public reference application uses TanStack Start and React 19 on Cloudflare Workers, typed server functions, Better Auth, Drizzle with Cloudflare D1, Zod, Polar, Resend, use-intl, Tailwind CSS 4, shadcn/ui, and Fumadocs. Bun and Vite+ handle development, checks, tests, and builds. The CLI configurator exposes the available alternatives for generated projects.
  - question: Why Better Auth instead of Clerk or Auth0?
    answer: >-
      Ownership and margin. Hosted auth charges per monthly active user, so your cost rises exactly
      when your revenue should. Better Auth lives in your codebase and your database, which means
      your users are rows you own, your auth logic is source you can read and extend, and there is no per-user authentication licence fee; hosting and database costs still depend on usage.
  - question: Can I use SaaSy Land for client projects?
    answer: >-
      The Agency tier at $899 includes an unlimited commercial client licence, so you can ship it to
      as many clients as you bill. The Core and Complete tiers cover your own products. All
      three are one-time payments with lifetime core updates.
  - question: Do I have to use every part of it?
    answer: >-
      No. The CLI asks configuration questions and writes only what you answered, so billing, content and
      email can each be left out with a --no-billing, --no-content or --no-email flag. Seven modules
      are unconditional and three are written only if you asked for them.
  - question: Is SaaSy Land open source?
    answer: >-
      The public website source code is MIT licensed; the separate CLI and private templates are proprietary. Purchase a licence, sign in at saasyland.com, and obtain the key required to install and use the CLI. Online licence validation applies to the CLI. Generated applications may be edited and deployed under the commercial licence and do not require a runtime key. SaaSy Land trademark rights are not granted by the website licence.
  - question: How do I know it is what this page says it is?
    answer: >-
      Check it before you pay. The documentation is public, the coverage report is generated by the
      repository itself rather than screenshotted, and the CI workflow that enforces the gate is a
      file in that repository. Every number on this page maps to a command or a path, listed in the
      verification table.
  - question: Why not just have an AI agent build this instead?
    answer: >-
      It will, and the result will look right. What it will not tell you is which auth edge cases it
      skipped, which webhook is not idempotent, or that the translation parity check it never wrote
      would have caught the missing keys. An agent's output is only as trustworthy as the checks
      that reviewed it, and on an empty repository there are none.
---

The three months at the start of every SaaS are always the same three months. Authentication with the edge cases. Billing with the webhook that must not charge twice. Transactional email. An admin nobody outside the company will ever see. Translations, timezones, currencies. A marketing site.

None of it is your product. All of it has been solved.

**SaaSy Land 2.0 is a production React SaaS starter that ships those three months as editable generated application source**, with a test suite already covering them and a coverage gate that fails the build if anything drops below 100%. One command scaffolds it. One payment buys it. Nothing bills you per user afterwards.

This post is the specification: what is in the box, what changed in 2.0, what it costs, and the command that proves each claim.

<Callout title="The short version">

- **Stack**: TanStack Start and React 19 on Cloudflare Workers, Vite+, Better Auth, Drizzle with Cloudflare D1, Zod, Polar, Resend, use-intl, Tailwind 4, shadcn/ui, Fumadocs, strict TypeScript.
- **Tests**: 176 Vitest files, 8 Playwright suites in real Chromium and WebKit, 100% coverage enforced as a CI gate.
- **Reach**: 9 locales, 156 currencies, 195 timezones, 248 countries, with a parity check that fails the build on a missing translation.
- **Cost**: $249 once. `$0` per monthly active user, forever.

</Callout>

_The public website now runs on TanStack Start and Cloudflare Workers. Better Auth handles accounts, Polar handles checkout and licence-key benefits, and Fumadocs supplies the blog and docs. The paid CLI is maintained separately._

## What is SaaSy Land?

SaaSy Land is a commercial React SaaS starter kit. A CLI asks configuration questions and writes only the modules you answered for: authentication on your own database, billing, transactional email, an admin console, internationalization and a visual page designer. It arrives with 176 test files and a coverage gate already enforcing them, so the code you receive is verified rather than merely present.

The public website source code is MIT licensed; the separate CLI and private templates are proprietary. Purchase a licence, sign in at saasyland.com, and obtain the key required to install and use the CLI. Online licence validation applies to the CLI. Generated applications may be edited and deployed under the commercial licence and do not require a runtime key. SaaSy Land trademark rights are not granted by the website licence.

## What is in the box

Ten modules. Seven are always written; three depend on how you answered the CLI.

| Module                   | What ships                                                                                                                                          |  Always  |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | :------: |
| **Authentication**       | Better Auth with email and password, OAuth, verification, password reset, two-factor and admin impersonation, on your database                      |   Yes    |
| **Database**             | Drizzle ORM against Cloudflare D1, typed end to end, with typed schemas, versioned migrations and isolated local tests                              |   Yes    |
| **Billing**              | Checkout, customer portal and webhooks, with products and pricing managed from the admin rather than hardcoded in the app                           | If asked |
| **Email**                | React Email templates through Resend, with the auth flows already sending real mail on day one                                                      | If asked |
| **Admin console**        | Thirteen sections built and styled: users, products, payments, analytics, blog, courses, pricing models, page designer, settings and more           |   Yes    |
| **Interface**            | 56 components on Tailwind 4 and shadcn/ui, in your repository rather than behind a package boundary you cannot edit                                 |   Yes    |
| **Internationalization** | 9 locales, 195 timezones, 156 currencies, 248 countries, plus a parity check that fails the build on a missing key                                  |   Yes    |
| **Content**              | An MDX blog and a documentation site on Fumadocs, kept in root-level content files, indexed and localized                                           | If asked |
| **Tests**                | 176 Vitest files, 8 Playwright suites, and 100% statement, branch, function and line coverage as a CI gate                                          |   Yes    |
| **Tooling**              | Bun, Vite+, strict TypeScript, Zod schemas, Cloudflare KV rate limiting, and a formatter, linter and type check that all run before anything merges |   Yes    |

No add-ons. No paid modules. No second checkout.

## Pick your stack, run one command

The CLI is not a template you download and then fight. It asks what you are building and writes exactly that, so the same tool scaffolds a single-tenant side project and a multi-tenant product with a merchant of record.

```bash
bunx saasyland@latest init my-app \
  --framework tanstack \
  --tenancy multi \
  --billing polar \
  --content mdx \
  --email resend
```

Some of the choices, with deliberately boring flags so the command survives being pasted into a script rather than into a prompt:

| Question  | Options                                           | Why it is a question                                                                                                                       |
| --------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Framework | `next`, `tanstack`                                | Both are supported; pick the one your team already knows                                                                                   |
| Tenancy   | `multi`, `single`                                 | Retrofitting organizations later touches every query you have written                                                                      |
| Billing   | `stripe`, `polar`, `lemonsqueezy`, `--no-billing` | Polar and Lemon Squeezy are merchants of record and carry EU VAT and US sales tax for you. Stripe does not. A founder makes that call once |
| Content   | `mdx`, `--no-content`                             | A blog and docs, or neither                                                                                                                |
| Email     | `resend`, `--no-email`                            | Transactional mail wired into the auth flows, or none                                                                                      |

Change an answer and the command changes with it. Run it again for the next project: the answers change, the CLI and the lifetime updates do not.

## The four things that are actually different

Plenty of starters ship auth and billing. These are the parts that are hard to copy, and each one is checkable.

<Cards>
  <Card title="A coverage gate, not a badge">
    100% of statements, branches, functions and lines, enforced on every pull request. Untested behaviour cannot merge, which is a different
    promise from tests existing somewhere in the repo.
  </Card>
  <Card title="$0 per monthly active user">
    Better Auth runs on your database. Your user table, your sessions, your margins. There is no per-user authentication licence fee.
    Hosting and database costs still depend on usage.
  </Card>
  <Card title="A page designer in the box">
    Landing pages, pricing pages and a blog your marketing team edits visually inside your own admin, rendering your own components. No
    second subscription.
  </Card>
  <Card title="The engineer, on video">
    Two courses in the Complete tier. One walks the codebase you receive, subsystem by subsystem. The other builds the whole product again
    from an empty folder.
  </Card>
</Cards>

### Why the coverage gate matters more now than it did at launch

When 2.0 shipped, the coverage number was a quality argument. It has since become a throughput argument, because a large share of the code being added to SaaS codebases is now written by an agent.

In the [2025 Stack Overflow Developer Survey](https://survey.stackoverflow.co/2025/ai), 84% of developers reported using or planning to use AI tools, while the most-cited frustration, named by 45%, was AI output that is "almost right, but not quite." A threshold that fails the build is the cheapest available answer to that: an agent cannot add behaviour without adding a test, and anything it breaks surfaces at the pull request rather than at a customer.

We wrote this up at length in [why your repository decides agent output quality](/blog/ai-coding-agents-repository-readiness) and in [how to choose a stack for AI coding agents](/blog/choosing-a-saas-stack-for-ai-coding-agents). The short version is that this repository scores 18 out of 20 on our own readiness scorecard, by construction rather than by effort.

## What changed in 2.0

| Area           | Before              | Now                                  | Why                                                                 |
| -------------- | ------------------- | ------------------------------------ | ------------------------------------------------------------------- |
| API layer      | Separate API server | TanStack Start server functions      | Typed request boundaries on Cloudflare Workers                      |
| Authentication | NextAuth            | Better Auth                          | Multi-tenancy, organizations, RBAC, and no per-user pricing         |
| Content        | Content Collections | Fumadocs                             | One system for the blog and the docs, both localized                |
| Localization   | 8 languages         | 9 locales                            | Plus 195 timezones, 156 currencies and a build-failing parity check |
| Interface      | Assorted components | 56 shadcn/ui components in your repo | Editable source rather than a package boundary                      |
| Tests          | Present             | 100%, enforced in CI                 | The gate, not the number                                            |
| Marketing site | None                | A visual page designer               | Ship the marketing, not only the app                                |

All of it is in the repository today. Lifetime core updates are included, so the next round lands in your repository too.

## What $249 actually buys

You are not buying code. You are buying back the calendar.

At $150 an hour, $249 is less than two hours of your time. The work it replaces is not three hours. It is authentication with two-factor and OAuth, a typed data layer, billing with webhooks that survive a retry, an admin console, a 9-locale catalog, a marketing site your team can edit, and a test suite that proves all of it still works after the next change.

| Tier         | Price         | For                                                       | Adds                                                                                                          |
| ------------ | ------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| **Core**     | $249 one-time | The founder who ships alone and reads every line          | Editable app source, CLI licence, private generator templates, the complete test suite, lifetime core updates |
| **Complete** | $399 one-time | The engineer who wants the reasoning, not just the result | Everything above, plus the full video course, architecture deep-dives and deployment playbooks                |
| **Agency**   | $899 one-time | The shop that bills clients for what it builds            | Everything above, plus an unlimited client licence, priority Discord and Figma files                          |

One-time payment in USD, with no subscription or monthly active user meter. Activation and any seat limits are disclosed before purchase.

## Check every claim before you pay

No testimonials, because there is nothing to verify in a quote. Every number on this page is generated by the public website repository, and here is where to find it.

| Claim                       | Where to check it                                                              |
| --------------------------- | ------------------------------------------------------------------------------ |
| 100% test coverage          | `coverage/coverage-summary.json`, generated by `bun run test:coverage`         |
| Enforced, not reported      | `.github/workflows/ci.yml`, on every pull request and push to main             |
| Real browsers, not jsdom    | `e2e/specs`, 8 Playwright suites on Chromium and WebKit                        |
| 9 locales, 156 currencies   | `messages/` and `src/modules/_core/constants`, checked by `bun run check:i18n` |
| Strict types, zero `any`    | `bun run check`, including strict type checking                                |
| 56 components, in your repo | `src/presentation/components`, editable source                                 |

## Who this is not for

If you want something free to learn on, clone a template and enjoy it. If you would rather a hosted vendor run your authentication and bill you per monthly active user for the privilege, buy that instead.

This is for people who intend to still own their margins at fifty thousand users, and who would rather find out at the pull request than in production.

<Callout title="Start here">

Read [the documentation](/docs) and the architecture section, then run the coverage report against the repository. When the numbers check out, [see what it costs](/#pricing).

</Callout>

## Frequently asked questions

<Accordions>

<Accordion title="What is SaaSy Land?">

SaaSy Land is a production React SaaS starter kit. A CLI asks configuration questions and scaffolds only what you answered: authentication on your own database, billing, transactional email, an admin console, 9-locale internationalization and a visual page designer, with 176 test files and a 100% coverage gate already enforcing them in CI. One payment of $249, no per-user fees.

</Accordion>

<Accordion title="How much does SaaSy Land cost?">

Three one-time tiers. Core is $249 and includes editable generated app source, a CLI licence and lifetime core updates. Complete is $399 and adds the full video course. Agency is $899 and adds an unlimited client licence, priority Discord support and Figma files. There is no subscription or monthly active user meter. Activation and any seat limits are disclosed before purchase.

</Accordion>

<Accordion title="Is SaaSy Land really 100% test coverage?">

Yes, on statements, branches, functions and lines, and it is a gate rather than a badge. 176 Vitest files plus 8 Playwright suites running in real Chromium and WebKit, with the threshold enforced on every pull request by `.github/workflows/ci.yml`. Run `bun run test:coverage` against the repository and the report regenerates itself.

</Accordion>

<Accordion title="What tech stack does SaaSy Land 2.0 use?">

The public reference application uses TanStack Start and React 19 on Cloudflare Workers, typed server functions, Better Auth, Drizzle with Cloudflare D1, Zod, Polar, Resend, use-intl, Tailwind CSS 4, shadcn/ui, and Fumadocs. Bun and Vite+ handle development, checks, tests, and builds. The CLI configurator exposes the available alternatives for generated projects.

</Accordion>

<Accordion title="Why Better Auth instead of Clerk or Auth0?">

Ownership and margin. Hosted auth charges per monthly active user, so your cost rises exactly when your revenue should. Better Auth lives in your codebase and your database, which means your users are rows you own, your auth logic is source you can read and extend, and there is no per-user authentication licence fee; hosting and database costs still depend on usage.

</Accordion>

<Accordion title="Can I use SaaSy Land for client projects?">

The Agency tier at $899 includes an unlimited commercial client licence, so you can ship it to as many clients as you bill. The Core and Complete tiers cover your own products. All three are one-time payments with lifetime core updates.

</Accordion>

<Accordion title="Do I have to use every part of it?">

No. The CLI asks configuration questions and writes only what you answered, so billing, content and email can each be left out with a `--no-billing`, `--no-content` or `--no-email` flag. Seven modules are unconditional and three are written only if you asked for them.

</Accordion>

<Accordion title="Is SaaSy Land open source?">

The public website source code is MIT licensed; the separate CLI and private templates are proprietary. Purchase a licence, sign in at saasyland.com, and obtain the key required to install and use the CLI. Online licence validation applies to the CLI. Generated applications may be edited and deployed under the commercial licence and do not require a runtime key. SaaSy Land trademark rights are not granted by the website licence.

</Accordion>

<Accordion title="How do I know it is what this page says it is?">

Check it before you pay. The documentation is public, the coverage report is generated by the repository itself rather than screenshotted, and the CI workflow that enforces the gate is a file in that repository. Every number on this page maps to a command or a path, listed in the verification table above.

</Accordion>

<Accordion title="Why not just have an AI agent build this instead?">

It will, and the result will look right. What it will not tell you is which auth edge cases it skipped, which webhook is not idempotent, or that the translation parity check it never wrote would have caught the missing keys. An agent's output is only as trustworthy as the checks that reviewed it, and on an empty repository there are none.

</Accordion>

</Accordions>

---

_Built by a professional developer to production standards. Questions go to hello@saasyland.com, and one person reads them._
