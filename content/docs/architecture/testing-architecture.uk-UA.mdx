---
title: "Тестування та CI"
description: "Проєкти Vitest, поріг покриття 100%, набір тестів Playwright і CI-пайплайн."
---

## Проєкти Vitest

`vp test` запускає Vitest через Vite+ із трьома проєктами, визначеними у `vite.config.ts`. Тести лежать у папці `__test__` поруч із кодом, який вони покривають, та імпортують тестові API з `vite-plus/test`.

| Проєкт        | Середовище | Шаблон файлів           |
| ------------- | ---------- | ----------------------- |
| `node`        | Node       | `*.test.ts`             |
| `integration` | Node       | `*.integration.test.ts` |
| `component`   | jsdom      | `*.component.test.tsx`  |

Жоден тест не звертається до справжнього облікового запису Cloudflare чи провайдера:

- D1 — це база даних SQLite у пам'яті, яка застосовує збережені в репозиторії міграції, а KV — мапа в пам'яті. KV і лічильники обмеження частоти запитів скидаються перед кожним тестом.
- `createServerFn` підмінено, тож серверна функція виконується в тому самому процесі через свої справжні middleware та валідатор.
- Компонентні тести рендеряться зі справжніми `QueryClient` і роутером.
- Запити до пошти, OAuth і платіжних сервісів замоковано.

## Покриття

`bun run test:coverage` вимагає 100% покриття рядків, інструкцій, функцій і гілок у всьому `src`. Примітиви shadcn, `src/platform/testing`, згенеровані файли й оголошення типів виключено. Покривайте реальну поведінку, а гілки, які ніколи не можуть виконатися, видаляйте, замість того щоб підміняти глобальні об'єкти, аби до них дістатися.

## Браузерні тести

Специфікації Playwright лежать у `e2e/specs`, page objects — у `e2e/pages`, фікстури — у `e2e/fixtures`. Вони запускаються на збірці, наближеній до production, яка використовує середовище Wrangler `test`: локальні D1 і KV у `.wrangler/test`, фіктивні секрети з `.env.test` і тестовий Worker, який перехоплює вихідні листи, щоб специфікація могла перейти за справжнім посиланням підтвердження. Локально Playwright сам застосовує міграції, наповнює базу, збирає й запускає застосунок. Набір виконується в Chromium і WebKit.

## Скрипти

```sh
bun run test
bun run test:unit
bun run test:integration
bun run test:component
bun run test:watch
bun run test:changed
bun run test:coverage
bun run test:e2e
bun run test:e2e:smoke
bun run test:e2e:pr
```

## CI

`.github/workflows/ci.yml` запускається для pull request і для пушів у `main`:

1. `bun run check`: форматування, лінтинг, перевірка типів і `check:i18n`
2. `bun run test:coverage`
3. Тестова збірка та набір Playwright у Chromium
4. Розгортання preview для pull request або розгортання production для `main`

## Пов'язані сторінки

- [Тестування](/docs/tooling/testing)
- [Playwright](/docs/tooling/playwright)
- [Написання тестів](/docs/guides/writing-tests)
