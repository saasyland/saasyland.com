---
title: "Forms & validation"
description: "React Hook Form, Zod, and server-side validation alignment."
---

## Pattern

- Zod schemas in the module (`*.zod.ts`)
- Client forms via RHF + resolvers
- Server actions parse the same schemas again (never trust the client)

## UI

shadcn fields + `validation-field-error` helpers under presentation.
