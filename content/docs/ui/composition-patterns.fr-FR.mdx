---
title: Modèles de composition
description: Préférez la composition par rapport aux accessoires booléens; soulevez l'état vers les fournisseurs.
---

Suivre `.agents/skills/vercel-composition-patterns`: composants composés, contexte générique (`state` / `actions` / `meta`), React 19 `use()`, non `forwardRef`.
