---
title: Padrões de composição
description: Preferir composição sobre adereços booleanos, elevar o estado em fornecedores.
---

Siga. `.agents/skills/vercel-composition-patterns`: componentes compostos, contexto genérico (`state` / `actions` / `meta`), React 19 `use()`, não `forwardRef`.
