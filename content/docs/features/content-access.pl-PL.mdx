---
title: "Dostęp do treści"
description: "Co dziś kontroluje dostęp: strażnicy tras, uprawnienia i rekord licencji. Nie ma treści premium za blokadą."
---

Nie ma treści premium za blokadą. Dostęp kontrolują:

- **Strażnicy tras**: `/app` wymaga sesji (`requireSignedIn`); `/admin` wymaga uprawnienia `admin: ["access"]` (`requireAdmin`).
- **Funkcje serwerowe**: `authorized(permission)` sprawdza uprawnienia ról z `auth.access.ts`.
- **Licencja**: tabela `license` przechowuje najwyżej jedną licencję na użytkownika z polami `tier` (`core`, `complete`, `agency`) i `status` (`active`, `revoked`), zapisywaną przez webhooki Polar. `/app` i `/app/license` odczytują ją przez `currentLicenseQuery`, aby pokazać opcje zakupu albo klucz licencyjny.

`/premium` (`src/routes/_landing.premium.tsx`) to strona zastępcza, która wyświetla tylko swój tytuł z `pages.premium`.

Aby udostępnić coś wyłącznie kupującym, odczytaj licencję na serwerze przez `getLicense(userId)` z `src/modules/license/use-cases/get-license.ts` i sprawdź `status` oraz `tier`.

## Powiązane strony

- [Płatności i licencje](/docs/features/payments)
- [Aplikacja klienta](/docs/features/customer-app)
