---
title: "Composition patterns"
description: "Prefer composition over boolean props; lift state into providers."
---

Follow `.agents/skills/vercel-composition-patterns`: compound components, generic context (`state` / `actions` / `meta`), React 19 `use()`, no `forwardRef`.
