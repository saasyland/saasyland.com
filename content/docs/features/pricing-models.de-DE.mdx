---
title: "Preismodelle"
description: "Nur Demo: die Ansicht /admin/pricing-models. Echte Preise stammen aus den Marketingdaten und aus Polar."
---

`/admin/pricing-models` rendert `PricingModelStats` und `PricingModelsGrid` aus Konstanten in ihren Dateien (`PRICING_STATS`, `PRICING_MODELS`). Die Filterschaltfläche und **Modell erstellen** sind nicht angebunden, und es wird nichts gespeichert.

## Woher die Preise stammen

- **Landingpage**: `TIER_PRICES` in `src/data/marketing.ts`, in US-Dollar: `core` 249, `complete` 399, `agency` 899. `Price` formatiert sie in der Locale der Seite.
- **Checkout**: die Polar-Produkte, auf die `POLAR_PRODUCT_ID_CORE`, `POLAR_PRODUCT_ID_COMPLETE` und `POLAR_PRODUCT_ID_AGENCY` verweisen.
- **Regionale Rabatte**: `PPP_TIERS` in `src/modules/_core/constants/pricing.ts` (Multiplikatoren 0,8, 0,7, 0,6 und 0,5 je nach Land), beim Checkout abgeglichen mit den in Polar konfigurierten automatischen Prozentrabatten.

Halten Sie `TIER_PRICES`, die Preise der Polar-Produkte und die Polar-Rabatte synchron; der Code vergleicht sie nicht.

## Verwandte Seiten

- [Zahlungen und Lizenzen](/docs/features/payments)
- [Landingpage](/docs/features/landing-page)
