---
title: Patrones de composición.
description: Prefiera la composición a las propiedades booleanas; eleve el estado a los proveedores.
---

Siga `.agents/skills/vercel-composition-patterns`: componentes compuestos, contexto genérico (`state` / `actions` / `meta`), React 19 `use()`, sin `forwardRef`.
