---
title: "Documentation"
description: "Architecture, guides, features and reference for the SaaSy Land application."
---

SaaSy Land is the source of the saasyland.com website, customer workspace and admin console: one TanStack Start application on Cloudflare Workers with D1, KV, Better Auth, Polar, Resend, use-intl and Fumadocs. The SaaSy Land CLI and its generator templates live in a separate, private repository.

<Callout title="How to use these docs">
  Start with **Getting started** to run the application. **Architecture** explains where code lives, **Guides** are task recipes,
  **Features** describe product behaviour and **Integrations** document each vendor adapter under `src/integrations`.
</Callout>

## Start here

<Cards>
  <Card title="Project" href="/docs/project/about">
    About the application, changelog, roadmap, contributing and licence.
  </Card>
  <Card title="Getting started" href="/docs/getting-started">
    Install Bun and Node, configure environment files, apply migrations and run locally.
  </Card>
  <Card title="Architecture" href="/docs/architecture">
    Routes, modules, integrations, presentation, request lifecycle and security.
  </Card>
  <Card title="Guides" href="/docs/guides">
    Add modules, use cases, admin pages, data tables, forms, emails and tests.
  </Card>
</Cards>

## Chapters

| Chapter         | Contents                                                                           |
| --------------- | ---------------------------------------------------------------------------------- |
| Project         | About, changelog, roadmap, contributing, licence                                   |
| Getting started | Prerequisites, installation, environment variables, first run, scripts, deployment |
| Architecture    | Directory structure, modules and use cases, request lifecycle, SSR, i18n, security |
| Guides          | Modules, use cases, admin pages, DataTable, forms, messages, tests, migrations     |
| Features        | Authentication, admin, products, payments, newsletter, blog, docs, locales         |
| Integrations    | TanStack Start, Better Auth, D1, KV, Workers, use-intl, Fumadocs, Resend, shadcn   |
| UI system       | shadcn components, DataTable, theme, fonts and branding, composition               |
| Tooling         | Environments, database, Vitest, Playwright, Vite+, CI/CD, scripts, commits         |
| Reference       | Routes, environment, conventions, modules, permissions, FAQ                        |

## Editing the docs

Pages live in `content/docs` as `<slug>.<locale>.mdx`, one file for each of the nine locales, and every folder lists its pages in `meta.<locale>.json`. All locales list the same slugs and keep code blocks byte-identical; `bun run check:i18n` enforces this through `scripts/check-content.ts`. `bun run content` regenerates `.source/`, and `dev` and every build script run it for you.

MDX pages can use Fumadocs components (`<Cards>`, `<Callout>`, `<Steps>`, `<Tabs>`, `<Accordions>`) and selected shadcn primitives (`<Button>`, `<Badge>`, `<Alert>`, `<Table>`). Internal links such as `/docs/tooling/testing` receive the reader's locale prefix automatically.
