---
title: "Accesso ai contenuti"
description: "Cosa controlla oggi l’accesso: guardie di rotta, permessi e record della licenza. Non esistono contenuti premium protetti."
---

Non esistono contenuti premium protetti. L’accesso è controllato da:

- **Guardie di rotta**: `/app` richiede una sessione (`requireSignedIn`); `/admin` richiede il permesso `admin: ["access"]` (`requireAdmin`).
- **Funzioni server**: `authorized(permission)` controlla i permessi dei ruoli definiti in `auth.access.ts`.
- **Licenza**: la tabella `license` contiene al massimo una licenza per utente con un `tier` (`core`, `complete`, `agency`) e uno `status` (`active`, `revoked`), scritta dai webhook di Polar. `/app` e `/app/license` la leggono tramite `currentLicenseQuery` per mostrare le opzioni di checkout oppure la chiave di licenza.

`/premium` (`src/routes/_landing.premium.tsx`) è una pagina segnaposto che mostra solo il titolo preso da `pages.premium`.

Per riservare qualcosa a chi ha acquistato, leggi la licenza sul server con `getLicense(userId)` da `src/modules/license/use-cases/get-license.ts` e controlla `status` e `tier`.

## Pagine correlate

- [Pagamenti e licenze](/docs/features/payments)
- [App cliente](/docs/features/customer-app)
