---
title: "Інтернаціоналізація та локалі"
description: "Як локалі відображаються в URL-адресах, як завантажуються простори імен перекладів і як компоненти їх використовують."
---

## Локалі та URL-адреси

SaaSy Land обслуговує дев'ять локалей: en-US (типова), de-DE, es-ES, fr-FR, it-IT, ja-JP, pl-PL, pt-BR і uk-UA. Типова локаль не має префікса (`/docs`); кожна інша локаль використовує свій повний тег (`/pl-PL/docs`). Worker перенаправляє префікси, що містять лише мову, і префікси типової локалі, як-от `/pl/docs` чи `/en-US/docs`, на канонічну URL-адресу.

`getCurrentLocale()` — ізоморфна функція. На сервері вона читає URL, а для запитів до `/api` і серверних функцій — cookie локалі. У браузері вона читає URL. Worker синхронізує cookie з URL-адресою сторінки. Серверні функції, які повертають локалізований вміст, отримують локаль як вхідний параметр і не покладаються на cookie.

## Повідомлення

Переклади лежать у `messages/<locale>/<namespace>.json`. Назва файлу — це простір імен, наприклад `pages.admin.users`, `components.custom.footer`, `emails.verify-email`, `auth.validations` або `common`. Простори імен із крапками об'єднуються в одне дерево повідомлень, тож `useTranslations("pages.admin.users")` читає `pages.admin.users.json`. Кожен простір імен сторінки має `metadata.title` і `metadata.description` для тегів head. Повідомлення — це звичайний JSON без згенерованих типів.

## Завантаження

- **Кореневі простори імен** (`common`, `components.*`, `errors*`) попередньо завантажуються в кореневому `beforeLoad`.
- **Маршрути сторінок** оголошують `const NAMESPACE = "pages.x"`, попередньо завантажують його в loader-функції та вказують у `staticData.namespaces`.
- **Маршрути макетів** оголошують `const NAMESPACES = [...] as const` і роблять те саме.
- **`TranslationsProvider`** обгортає тіло документа в кореневій оболонці. Він поєднує кореневі простори імен зі `staticData.namespaces` зіставлених маршрутів і передає результат в `IntlProvider`, тож перекладено кожен екран, включно з екранами помилок.
- **Листи** завантажують свій простір імен на сервері через `loadNamespace` у сценарії використання, який їх надсилає.

## `src/integrations/use-intl`

| Файл                 | Експорти                                                                                            |
| -------------------- | --------------------------------------------------------------------------------------------------- |
| `i18n.config.ts`     | `I18N`: `SUPPORTED_LOCALES`, `DEFAULT_LOCALE`, `COOKIE_NAME`, `DEFAULT_TIMEZONE`                    |
| `i18n.messages.ts`   | `loadNamespace`, `loadPageMetadata`, `messagesQueryOptions`, `preloadNamespaces`, `ROOT_NAMESPACES` |
| `i18n.middleware.ts` | `resolveLocale`, яку Worker використовує для перенаправлень і cookie                                |
| `i18n.paths.ts`      | `localizePathname`, `deLocalizePathname`, `canonicalizePathname`, `localeLinks`                     |
| `i18n.utils.ts`      | `getCurrentLocale`, `localizeUrl`, `deLocalizeUrl`, `parseLocaleCookie`                             |

## Посилання

Посиланням роутера не потрібна обробка локалі: `<Link to={ROUTES.SIGN_IN}>` отримує префікс від `rewrite` роутера. `localizePathname` потрібна лише для сирих URL-адрес, що оминають роутер, як-от значення `callbackURL` для Better Auth, посилання в листах і вручну зібрані елементи `<a href>`:

```ts
const callbackURL = localizePathname({ locale: getCurrentLocale(), pathname: `${ROUTES.VERIFY_EMAIL}?verified=true` })
```

Перемикач локалі рендерить звичайне посилання на той самий шлях у кожній локалі, тож зміна мови завантажує новий документ.

## Переклади в компонентах

Компонент викликає `useTranslations` рівно один раз і називає функцію перекладу `t`. Після кожного наступного виклику `useTranslations` i18n-ally змінює область усіх подальших викликів `t()`, а `bun run check:i18n` завершується помилкою на ключах, які не розв'язуються у своїй області. Компонент, якому потрібні два непов'язані простори імен, використовує їхній спільний батьківський простір імен або `useTranslations()` з повними ключами.

`bun run check:i18n` запускає три скрипти: `check-i18n.ts` (паритет ключів між локалями та розв'язання ключів у їхній області), `check-message-formats.ts` (синтаксис повідомлень ICU) і `check-content.ts` (паритет документації та блогу між локалями).

## Пов'язані сторінки

- [use-intl](/docs/integrations/use-intl)
- [Повідомлення перекладів](/docs/guides/i18n-messages)
- [Інтернаціоналізація](/docs/features/internationalization)
