---
title: "Przepływy uwierzytelniania"
description: "Jak działają logowanie, rejestracja z weryfikacją adresu e-mail, reset hasła, uwierzytelnianie dwuskładnikowe, OAuth i wylogowanie."
---

Better Auth (`src/integrations/better-auth/auth.server.ts`) przechowuje użytkowników, sesje i tokeny weryfikacyjne w D1 i obsługuje swoje endpointy pod `/api/auth`. Strony uwierzytelniania znajdują się pod layoutem `/auth`, a ich formularze w `src/presentation/components/custom/auth/`.

## Strażnicy tras

`src/integrations/better-auth/auth.routes.ts` eksportuje strażników używanych w `beforeLoad`:

| Strażnik             | Używany w                        | Działanie                                                             |
| -------------------- | -------------------------------- | --------------------------------------------------------------------- |
| `requireSignedIn`    | layout `/app`                    | Bez sesji przekierowuje na `/auth/sign-in`                            |
| `requireAdmin`       | layout `/admin`                  | Dodatkowo przekierowuje na `/app` bez uprawnienia `admin: ["access"]` |
| `redirectIfSignedIn` | `/auth/sign-in`, `/auth/sign-up` | Kieruje zalogowanego użytkownika na `/admin` lub `/app`               |
| `redirectAfterAuth`  | `/auth/callback`                 | Kieruje nową sesję na `/admin` lub `/app` zależnie od roli            |

`requireSignedIn` i `requireAdmin` sprawdzają sesję ponownie przy każdej nawigacji, `redirectIfSignedIn` korzysta z sesji z cache, a każdy strażnik czyści cache zapytań, gdy zmienia się zalogowany użytkownik.

## Logowanie

1. `SignInForm` wywołuje `signIn.email` z klienta Better Auth.
2. Niezweryfikowane konto dostaje błąd `EMAIL_NOT_VERIFIED` i trafia na `/auth/verify-email` ze swoim adresem e-mail.
3. Konto z uwierzytelnianiem dwuskładnikowym dostaje `twoFactorRedirect`; wtyczka klienta otwiera wtedy `/auth/two-factor` w bieżącym języku.
4. Po sukcesie `usePostAuthRedirect` czyści cache zapytań i routera, a następnie przechodzi na `/auth/callback`, które przekierowuje zależnie od roli.

Błędy, które Better Auth zgłasza przez przekierowanie, trafiają jako `?error=` na `/auth/sign-in` i są wyświetlane z `auth.errors`.

## Rejestracja i weryfikacja adresu e-mail

1. `SignUpForm` waliduje `signUpWithPasswordSchema` i wywołuje `signUpWithPasswordMutation`, funkcję serwerową z limitem żądań, która wywołuje `auth.api.signUpEmail`.
2. Better Auth tworzy użytkownika i wywołuje `sendVerifyEmail`, który wysyła link do `/auth/verify-email?token=…` w języku strony, na której rozpoczęto rejestrację. Ponowna rejestracja z niezweryfikowanym adresem wysyła nowy link.
3. Formularz przechodzi na `/auth/verify-email?email=…`, gdzie `VerifyEmailForm` może wysłać link ponownie przez `sendVerificationEmailMutation` z limitem żądań.
4. Otwarcie linku: `beforeLoad` trasy przekazuje token do `/api/auth/verify-email`. Better Auth go weryfikuje, loguje użytkownika i wraca na `/auth/verify-email?verified=true`, które przekierowuje na `/auth/callback`. Niepoprawny lub wygasły link wraca z `error`, a strona proponuje nowy.

Logowanie jest zablokowane do czasu weryfikacji adresu e-mail (`requireEmailVerification: true`).

## Reset hasła

1. `/auth/forgot-password`: `ForgotPasswordForm` wywołuje `requestPasswordResetMutation` z `redirectTo` ustawionym na zlokalizowaną ścieżkę `/auth/reset-password`.
2. Better Auth wywołuje `sendResetPasswordEmail`. Link z e-maila przechodzi przez Better Auth, które przekierowuje na `/auth/reset-password?token=…` albo z `error`, gdy token jest niepoprawny lub wygasł.
3. `ResetPasswordForm` wysyła nowe hasło przez `resetPasswordMutation`. Better Auth unieważnia wszystkie istniejące sesje (`revokeSessionsOnPasswordReset: true`), a formularz przechodzi do logowania.

## Uwierzytelnianie dwuskładnikowe

- Włączanie (ustawienia panelu administracyjnego, sekcja Bezpieczeństwo): `TwoFactorDialog` prosi o hasło (`enableTwoFactorMutation`), pokazuje sekret TOTP do dodania w aplikacji uwierzytelniającej, potwierdza jeden kod (`verifyTotpMutation`), a potem pokazuje kody zapasowe. `disableTwoFactorMutation` wyłącza tę funkcję.
- Logowanie: `/auth/two-factor` przyjmuje kod z aplikacji uwierzytelniającej (`verifyTotpMutation`) lub kod zapasowy (`verifyBackupCodeMutation`), oba z limitem żądań, z opcją zaufania urządzeniu. Po sukcesie uruchamia się `usePostAuthRedirect`.

## OAuth

`OAuthButtons` wywołuje `authClient.signIn.social` dla Google lub GitHuba z `callbackURL` ustawionym na zlokalizowaną ścieżkę `/auth/callback` i `errorCallbackURL` na zlokalizowaną ścieżkę `/auth/sign-in`. Dostawców konfiguruje `socialProviders`; zarejestruj u nich `https://<host>/api/auth/callback/google` i `https://<host>/api/auth/callback/github`. Konta z tym samym adresem e-mail łączą się automatycznie, a tokeny dostawców są przechowywane w postaci zaszyfrowanej.

## Wylogowanie

`useSignOut` wywołuje `signOutUserMutation`, czyści cache zapytań, przechodzi na `/auth/sign-in` i czyści cache routera.

## Powiązane poradniki

- [Lista kontrolna bezpieczeństwa](/docs/guides/security-checklist)
- [Role i uprawnienia](/docs/guides/roles-and-permissions)
- [Szablony e-maili](/docs/guides/email-templates)
