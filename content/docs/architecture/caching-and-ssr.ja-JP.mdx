---
title: "キャッシュと SSR"
description: "データをキャッシュする場所：リクエストごとのクエリキャッシュ、プリレンダリング済みのページ、KV、D1。"
---

## クエリキャッシュ

`getRouter` はリクエストごとに新しい `QueryClient` を作成するため、サーバー上でユーザー間にデータが漏れることはありません。デフォルト値は次のとおりです。

| 設定                          | 値           |
| ----------------------------- | ------------ |
| クエリの `staleTime`          | 1 分         |
| クエリの `gcTime`             | 5 分         |
| ルーターの `defaultStaleTime` | 1 分         |
| プリロード                    | インテント時 |

ローダーは `queryClient.query({ ...options, staleTime: "static" })` でキャッシュを埋め、コンポーネントは `useSuspenseQuery` で同じキーを読み取ります。`setupRouterSsrQueryIntegration` はサーバーのキャッシュを HTML にシリアライズし、ブラウザでハイドレートします。

- **翻訳メッセージ**は、ロケールと名前空間ごとに、`staleTime` と `gcTime` を無限に設定してキャッシュされます。
- **セッションクエリ**（`getCurrentSessionQuery`）の `staleTime` は 1 分です。ルートガードは実際のナビゲーション時にこれを再取得し、サインイン中のユーザーが変わるとキャッシュ全体をクリアします。
- **ミューテーション**は成功後、そのモジュールのクエリキー（例：`USER_QUERY_KEYS.ALL`）を無効化します。

## プリレンダリング

`bun run build` はリンクをクロールして公開ページをプリレンダリングします。対象は、9 つすべてのロケールのランディング、法務、ブログ、ドキュメントの各ページです。`/admin`、`/app`、`/auth`、`/newsletter`、`/api` 配下のパス（ロケールプレフィックスの有無を問わない）と、クエリ文字列やハッシュを含むリンクは除外されます。

Worker は、アプリケーションを読み込む前に、`ASSETS` バインディングからプリレンダリング済みの HTML を返します。プリレンダリングされたページにはユーザーデータが含まれないため、誰に返しても安全です。訪問者が地域別価格の対象になる場合、Worker は HTML を書き換え、レスポンスに `Cache-Control: private, no-cache` を設定します。

## KV と D1

- **KV（`CACHE`）** に保存するのは、`src/lib/github.ts` 用の GitHub スター数だけです。1 時間より古いキャッシュ値はそのまま即座に返され、`waitUntil` によってバックグラウンドで更新されます。
- **D1（`DB`）** にはセッションとレート制限のカウンターを保存します。サーバー関数のセッションチェックは Better Auth のクッキーキャッシュを経由せず、結果はリクエストの残りの処理の間メモ化されます。
- サーバー関数が、キャッシュされたデータをもとに権限を判断することはありません。

## 関連ページ

- [リクエストのライフサイクル](/docs/architecture/request-lifecycle)
- [Cloudflare KV](/docs/integrations/cloudflare-kv)
- [TanStack Start](/docs/integrations/tanstack-start)
