---
title: "料金モデル"
description: "デモのみ: /admin/pricing-models 画面。実際の価格はマーケティングデータと Polar から来ます。"
---

`/admin/pricing-models` は、各ファイル内の定数（`PRICING_STATS`、`PRICING_MODELS`）から `PricingModelStats` と `PricingModelsGrid` を表示します。フィルターと **モデルを作成** のボタンは未接続で、何も保存されません。

## 価格の出どころ

- **ランディングページ**: `src/data/marketing.ts` の `TIER_PRICES`（米ドル）。`core` 249、`complete` 399、`agency` 899 で、`Price` がページの言語で書式設定します。
- **購入**: `POLAR_PRODUCT_ID_CORE`、`POLAR_PRODUCT_ID_COMPLETE`、`POLAR_PRODUCT_ID_AGENCY` が参照する Polar の商品。
- **地域割引**: `src/modules/_core/constants/pricing.ts` の `PPP_TIERS`（国に応じた倍率 0.8、0.7、0.6、0.5）。購入時に Polar で設定した自動のパーセント割引と照合されます。

`TIER_PRICES`、Polar の商品価格、Polar の割引は手動で一致させてください。コードはこれらを比較しません。

## 関連ページ

- [決済とライセンス](/docs/features/payments)
- [ランディングページ](/docs/features/landing-page)
