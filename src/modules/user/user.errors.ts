/** Full next-intl paths stored on domain errors — translate on the client with `useTranslations("user.errors")`. */
export const USER_ERROR_MESSAGE = {
  notFound: "user.errors.notFound",
} as const
