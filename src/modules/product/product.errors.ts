/** Full next-intl paths stored on domain errors — translate on the client with `useTranslations("product.errors")`. */
export const PRODUCT_ERROR_MESSAGE = {
  notFound: "product.errors.notFound",
} as const
