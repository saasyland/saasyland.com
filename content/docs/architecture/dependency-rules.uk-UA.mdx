---
title: "Правила залежностей"
description: "Які папки можуть імпортувати які та як суто серверний код не потрапляє в браузерний бандл."
---

## Напрямки імпорту

| Папка                     | Може імпортувати                                                                                                  | Не має імпортувати                                         |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `src/routes`              | представлення, опції запитів і мутацій модулів, інтеграції (guard-функції, i18n, Fumadocs), `src/data`, `src/lib` | клієнти бази даних або провайдерів поза серверною функцією |
| `src/presentation`        | опції модулів, Zod-схеми, константи й типи, хуки, `src/data`, `src/lib`, безпечні для браузера інтеграції         | `db`, `auth` з `auth.server.ts`, Polar, Resend             |
| `src/modules/*/use-cases` | інтеграції (`db`, `auth`, `sendEmail`, `polar`), `_core`, схеми модулів, шаблони листів                           | компоненти                                                 |
| `src/integrations`        | `_core`, схеми модулів і сценарії використання, до яких звертається вендор через зворотні виклики                 | компоненти                                                 |
| `src/server.ts`           | допоміжні функції локалей, cookie, константи `_core`, ресурси документа                                           | базу даних, Better Auth або Polar SDK                      |

До безпечних для браузера інтеграцій належать `i18n.paths.ts`, `i18n.utils.ts`, `auth.client.ts`, `auth.access.ts` і `form.fields.ts`.

## Межі суто серверного коду

- **Серверні функції.** TanStack Start вилучає обробник кожного `createServerFn` із браузерного бандла й замінює виклик RPC-запитом. Саме тому компонент може імпортувати `createCategoryMutation` з того самого файлу, який імпортує `db`.
- **Суто серверні модулі.** Файли, що читають секрети або прив'язки, починаються з `import "@tanstack/react-start/server-only"`, і це тримає їх поза клієнтською збіркою. Приклади: `drizzle.database.ts`, `auth.server.ts`, `polar.config.ts`, `resend.utils.ts`, `src/lib/rate-limit.ts` і `i18n.middleware.ts`.
- **Функції для конкретного середовища.** `createServerOnlyFn` обгортає код, який виконується лише на сервері (`getRequestSession`). `createIsomorphicFn` надає окремі реалізації для сервера й браузера (`getCurrentLocale`, `getCookie`).
- **Прив'язки та секрети** надходять з `env` у `cloudflare:workers` і читаються лише всередині суто серверних модулів або обробників серверних функцій.
- **Точка входу Worker.** `src/server.ts` виконується для кожного запиту, зокрема для статичних файлів. Його статичні імпорти мінімальні, а обробник TanStack Start завантажується динамічним `import()` лише тоді, коли не знайдено відповідного статичного ресурсу.

```ts
import "@tanstack/react-start/server-only"

import { env } from "cloudflare:workers"

import { drizzle } from "drizzle-orm/d1"

import * as schema from "~/src/integrations/drizzle-orm/drizzle.schemas"

export const db = drizzle(env.DB, { schema })
```

## Пов'язані сторінки

- [Структура каталогів](/docs/architecture/directory-structure)
- [Серверні функції та сценарії використання](/docs/architecture/modules-and-use-cases)
- [Життєвий цикл запиту](/docs/architecture/request-lifecycle)
