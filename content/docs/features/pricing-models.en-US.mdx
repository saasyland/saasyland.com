---
title: "Pricing models"
description: "Demo only: the /admin/pricing-models screen. Real prices come from marketing data and Polar."
---

`/admin/pricing-models` renders `PricingModelStats` and `PricingModelsGrid` from constants in their files (`PRICING_STATS`, `PRICING_MODELS`). The filter and **Create model** buttons are not wired and nothing is stored.

## Where prices come from

- **Landing page**: `TIER_PRICES` in `src/data/marketing.ts`, in US dollars: `core` 249, `complete` 399, `agency` 899. `Price` formats them in the page locale.
- **Checkout**: the Polar products referenced by `POLAR_PRODUCT_ID_CORE`, `POLAR_PRODUCT_ID_COMPLETE` and `POLAR_PRODUCT_ID_AGENCY`.
- **Regional discounts**: `PPP_TIERS` in `src/modules/_core/constants/pricing.ts` (multipliers 0.8, 0.7, 0.6 and 0.5 by country), matched at checkout against automatic percentage discounts configured in Polar.

Keep `TIER_PRICES`, the Polar product prices and the Polar discounts in sync; the code does not compare them.

## Related pages

- [Payments and licences](/docs/features/payments)
- [Landing page](/docs/features/landing-page)
