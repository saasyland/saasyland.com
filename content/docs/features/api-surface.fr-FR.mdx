---
title: "Gestionnaires HTTP TanStack Start"
description: "Les routes serveur sous /api et leur lien avec les fonctions serveur."
---

L’application expose trois routes HTTP, déclarées avec `server.handlers` sur `createFileRoute` dans `src/routes/api` :

| Route         | Méthodes  | Gestionnaire                                                                                                                                                                     |
| ------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/api`        | GET       | Renvoie `{ "runtime": "cloudflare-workers" }`.                                                                                                                                   |
| `/api/auth/$` | GET, POST | Better Auth : connexion, retours OAuth, `/api/auth/verify-email`, ainsi que les points de terminaison du plugin Polar `/api/auth/polar/webhooks` et `/api/auth/customer/portal`. |
| `/api/search` | GET       | Recherche Fumadocs (`fumadocs.search.ts`).                                                                                                                                       |

```ts
import { createFileRoute } from "@tanstack/react-router"

import { auth } from "~/src/integrations/better-auth/auth.server"

export const Route = createFileRoute("/api/auth/$")({
  server: { handlers: { GET: ({ request }) => auth.handler(request), POST: ({ request }) => auth.handler(request) } },
})
```

## Des fonctions serveur plutôt que REST

Les opérations de l’application sont des fonctions `createServerFn` dans `src/modules/<table>/use-cases` (ou directement dans l’unique route qui les utilise). TanStack Start les appelle en RPC sous `/_serverFn`, avec des middlewares pour les sessions, les permissions et les limites de débit. N’ajoutez pas de point de terminaison REST pour une opération que seule cette application appelle ; ajoutez une route HTTP uniquement pour les appelants externes comme les webhooks, et vérifiez leurs signatures dans l’intégration du fournisseur.

## Comportement

- Le Worker n’applique pas de redirection de langue à `/api` ni à `/_serverFn` ; ces requêtes prennent la langue dans le cookie.
- Le build ne prérend pas `/api`.
- Les tests des gestionnaires se trouvent dans `src/routes/api/__test__/handlers.test.ts`.

## Guides associés

- [Ajouter une route d’API](/docs/guides/add-an-api-route)
- [Fonctions serveur](/docs/guides/server-functions)
