---
title: Modelli di composizione
description: Preferire la composizione su oggetti booleani; sollevare lo stato in fornitori.
---

Seguici `.agents/skills/vercel-composition-patterns`: componenti composti, contesto generico (`state` / `actions` / `meta`), React 19 `use()`No. `forwardRef`.
