---
title: "Dodaj use-case"
description: "Utwórz akcję serwerową z uprawnieniami w module."
---

> Pełna treść jest utrzymywana po angielsku i może być uzupełniana sukcesywnie. Poniżej znajduje się lokalna wersja / mapa tej strony.

## Steps

1. Add `modules/<domain>/use-cases/<verb>-<noun>.use-case.ts`
2. Use `authedActionClient(PERMISSIONS…)` from next-safe-action wiring
3. Validate input with module Zod schemas
4. Call auth/DB; map failures to domain errors
5. Add `__test__` covering happy path + forbidden
6. Export and call from the page/action UI

## Naming

`list-users.use-case.ts`, `ban-user.use-case.ts` — verb-first, kebab-case.
