---
title: "Routes"
description: "Every page and API path, its ROUTES constant, its route file and its guard."
---

Every path has a constant in `ROUTES` (`src/routes.ts`). Links use the constant, for example `<Link to={ROUTES.SIGN_IN}>`, and the router adds the locale prefix. Each page below is also served under every non-default locale prefix, for example `/pl-PL/terms`.

## Public pages

These pages use the `_landing` layout (navigation and footer), except blog and docs, which have their own layouts. All of them are prerendered except the newsletter pages.

| Path                      | Constant                 | Route file                            |
| ------------------------- | ------------------------ | ------------------------------------- |
| `/`                       | `HOME`                   | `_landing.index.tsx`                  |
| `/premium`                | `PREMIUM`                | `_landing.premium.tsx`                |
| `/licence`                | `LICENCE`                | `_landing.licence.tsx`                |
| `/privacy`                | `PRIVACY`                | `_landing.privacy.tsx`                |
| `/terms`                  | `TERMS`                  | `_landing.terms.tsx`                  |
| `/refunds`                | `REFUNDS`                | `_landing.refunds.tsx`                |
| `/newsletter/confirm`     | `NEWSLETTER_CONFIRM`     | `_landing.newsletter.confirm.tsx`     |
| `/newsletter/unsubscribe` | `NEWSLETTER_UNSUBSCRIBE` | `_landing.newsletter.unsubscribe.tsx` |
| `/blog`                   | `BLOG`                   | `blog.tsx`, `blog.index.tsx`          |
| `/blog/*`                 | none                     | `blog.$.tsx`                          |
| `/docs`                   | `DOCS`                   | `docs.tsx`, `docs.index.tsx`          |
| `/docs/*`                 | none                     | `docs.$.tsx`                          |

## Authentication

The `/auth` layout (`auth.tsx`) has no guard; individual pages do.

| Path                    | Constant          | Route file                 | Guard                                                      |
| ----------------------- | ----------------- | -------------------------- | ---------------------------------------------------------- |
| `/auth/sign-in`         | `SIGN_IN`         | `auth.sign-in.tsx`         | `redirectIfSignedIn`                                       |
| `/auth/sign-up`         | `SIGN_UP`         | `auth.sign-up.tsx`         | `redirectIfSignedIn`                                       |
| `/auth/forgot-password` | `FORGOT_PASSWORD` | `auth.forgot-password.tsx` | none                                                       |
| `/auth/reset-password`  | `RESET_PASSWORD`  | `auth.reset-password.tsx`  | none                                                       |
| `/auth/two-factor`      | `TWO_FACTOR`      | `auth.two-factor.tsx`      | none                                                       |
| `/auth/verify-email`    | `VERIFY_EMAIL`    | `auth.verify-email.tsx`    | Redirects a `token` to Better Auth's verification endpoint |
| `/auth/callback`        | `AUTH_CALLBACK`   | `auth.callback.tsx`        | `redirectAfterAuth`                                        |

## Customer app

The `/app` layout (`app.tsx`) uses `requireSignedIn`.

| Path           | Constant      | Route file        |
| -------------- | ------------- | ----------------- |
| `/app`         | `APP`         | `app.index.tsx`   |
| `/app/license` | `APP_LICENSE` | `app.license.tsx` |

## Admin

The `/admin` layout (`admin.tsx`) uses `requireAdmin`.

| Path                       | Constant                  | Route file                                                                          |
| -------------------------- | ------------------------- | ----------------------------------------------------------------------------------- |
| `/admin`                   | `ADMIN`                   | `admin.index.tsx`                                                                   |
| `/admin/analytics`         | `ADMIN_ANALYTICS`         | `admin.analytics.tsx`                                                               |
| `/admin/blog`              | `ADMIN_BLOG`              | `admin.blog.index.tsx`                                                              |
| `/admin/blog/create`       | `ADMIN_BLOG_CREATE`       | `admin.blog.create.tsx`                                                             |
| `/admin/courses/create`    | `ADMIN_COURSES_CREATE`    | `admin.courses.create.tsx`                                                          |
| `/admin/landing-page`      | `ADMIN_LANDING_PAGE`      | `admin.landing-page.tsx`                                                            |
| `/admin/payments`          | `ADMIN_PAYMENTS`          | `admin.payments.tsx`                                                                |
| `/admin/pricing-models`    | `ADMIN_PRICING`           | `admin.pricing-models.tsx`                                                          |
| `/admin/products`          | `ADMIN_PRODUCTS`          | `admin.products.index.tsx`                                                          |
| `/admin/products/create`   | `ADMIN_PRODUCTS_CREATE`   | `admin.products.create.tsx`                                                         |
| `/admin/settings`          | `ADMIN_SETTINGS`          | `admin.settings.tsx`                                                                |
| `/admin/users`             | `ADMIN_USERS`             | `admin.users.tsx` (layout), `admin.users.index.tsx` redirects to `/admin/users/all` |
| `/admin/users/all`         | `ADMIN_USERS_ALL`         | `admin.users.all.tsx`                                                               |
| `/admin/users/invitations` | `ADMIN_USERS_INVITATIONS` | `admin.users.invitations.tsx`                                                       |
| `/admin/users/roles`       | `ADMIN_USERS_ROLES`       | `admin.users.roles.tsx`                                                             |
| `/admin/users/security`    | `ADMIN_USERS_SECURITY`    | `admin.users.security.tsx`                                                          |

## API

API paths are never localized.

| Path          | Constant        | Route file      |
| ------------- | --------------- | --------------- |
| `/api`        | `API`           | `api/index.ts`  |
| `/api/auth/*` | `API_AUTH_BASE` | `api/auth.$.ts` |
| `/api/search` | none            | `api/search.ts` |

`ROUTES.API_AUTH` lists Better Auth endpoint paths relative to `/api/auth`; `auth.server.ts` uses them to exempt endpoints from rate limiting. `API_AUTH_CUSTOMER_PORTAL` and `API_AUTH_VERIFY_EMAIL` are full paths to Better Auth endpoints. `AUTH` and `NEWSLETTER` have no page of their own; together with `ADMIN`, `APP` and `API` they mark the areas that are never prerendered.

## Related pages

- [Routes and delivery](/docs/architecture/delivery-layer)
- [Add an admin page](/docs/guides/add-an-admin-page)
