/** Full next-intl paths stored on domain errors — translate on the client with `useTranslations("category.errors")`. */
export const CATEGORY_ERROR_MESSAGE = {
  notFound: "category.errors.notFound",
} as const
