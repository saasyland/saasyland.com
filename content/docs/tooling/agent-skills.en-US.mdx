---
title: "Agent skills"
description: "`.agents/skills` and how agents should use them."
---

## Location

`.agents/skills/<vendor>/<topic>/SKILL.md` (+ references). Run `bun run skills:sync` to restore the pinned skill set and vendor groups.

## Rule

When a task touches TanStack Start, Better Auth, D1, routing, Cloudflare Workers, or another integration, **read the matching skill first**. Skills encode this repo’s conventions beyond generic training data.

Also see `AGENTS.md`
