---
title: "Two-factor authentication"
description: "TOTP enablement, verification, and backup codes."
---

## Module

`modules/two-factor/use-cases` — enable, disable, verify TOTP, verify backup code.

## UX

Settings security tab + `/auth/two-factor` challenge route. Follow Better Auth 2FA plugin constraints in `auth.constraints.ts`.
